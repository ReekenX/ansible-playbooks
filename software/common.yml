- hosts: all
  tasks:
    - name: Install common software
      apt: pkg={{ item }} state=latest update_cache=true
      remote_user: root
      with_items:
        - elinks
        - git
        - htop
        - iftop
        - iotop
        - nmap
        - ntp
        - tree
        - sl
        - whois
